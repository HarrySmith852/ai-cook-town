---
name: grill
description: Run a relentless interview to stress-test a plan, spec, or decision before committing to it
argument-hint: [the plan, decision, or /spec output to grill]
---

Use the Skill tool with skill "grilling" to relentlessly interview the user about the following, per the grilling skill's process (design-tree, frontier-by-round, dispatch sub-agents for facts rather than asking the user, don't act until the frontier is empty and the user confirms shared understanding): $ARGUMENTS
